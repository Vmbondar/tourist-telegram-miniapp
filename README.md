# Tourist Telegram Mini App

Telegram Mini App для туристов с системой подписок для бизнесов.

## Технологический стек

- **Backend**: FastAPI (Python 3.11+)
- **Frontend**: React (TypeScript) + Telegram Mini App SDK
- **База данных**: PostgreSQL
- **Файловое хранилище**: Yandex Object Storage
- **Платежная система**: ЮKassa
- **Админ-панель**: SQLAdmin (FastAPI Admin)
- **Аутентификация**: Telegram Web App Authentication (initData) + Email

## Структура проекта

```
Bot_2rist/
├── backend/                 # FastAPI backend
│   ├── app/
│   │   ├── api/            # API эндпоинты
│   │   │   └── v1/
│   │   │       └── auth.py # Аутентификация
│   │   ├── core/           # Ядро приложения
│   │   │   ├── config.py   # Конфигурация
│   │   │   ├── security.py # Безопасность
│   │   │   └── dependencies.py # Зависимости
│   │   ├── db/             # Работа с БД
│   │   │   ├── models/     # SQLAlchemy модели
│   │   │   ├── base.py     # Базовые модели
│   │   │   └── session.py  # Сессии БД
│   │   ├── schemas/        # Pydantic схемы
│   │   ├── services/       # Бизнес-логика
│   │   ├── admin/          # Админ-панель
│   │   └── main.py         # Точка входа
│   ├── alembic/            # Миграции БД
│   ├── requirements.txt    # Зависимости
│   └── Dockerfile          # Docker образ
├── frontend/               # React frontend (в разработке)
├── docker-compose.yml      # Docker Compose конфигурация
└── README.md              # Документация
```

## 🚀 Быстрый старт

### Автоматический запуск (рекомендуется):

```bash
./start.sh
```

Этот скрипт автоматически запустит:
- ✅ Backend API (порт 8000)
- ✅ Frontend (порт 3000)
- ✅ Публичные туннели для доступа из Telegram

### Остановка всех сервисов:

```bash
./stop.sh
```

---

## 📋 Установка и настройка

### Предварительные требования

- Node.js 16+ и npm (для Frontend)
- Python 3.11+ (для Backend)
- PostgreSQL 15+
- Docker и Docker Compose (опционально)

### Настройка переменных окружения

**📋 Подробные инструкции по настройке всех ключей и доступов см. в файле [SETUP_REQUIRED.md](SETUP_REQUIRED.md)**

1. Скопируйте файл `env.example` в `.env`:
```bash
cd backend
cp env.example .env
```

2. **Обязательные переменные для запуска:**
   - `SECRET_KEY` - секретный ключ для JWT токенов (сгенерируйте командой: `python scripts/generate_secret_key.py`)
   - `TELEGRAM_BOT_TOKEN` - токен Telegram бота от [@BotFather](https://t.me/BotFather)
   - `TELEGRAM_SECRET_KEY` - секретный ключ для проверки initData (для начала можно тот же, что и токен)

3. Опциональные переменные (для следующих этапов):
   - Yandex Object Storage (для Этапа 4)
   - ЮKassa (для Этапа 5)

### Запуск с Docker Compose

1. Запустите все сервисы:
```bash
docker-compose up -d
```

2. Примените миграции БД:
```bash
docker-compose exec backend alembic upgrade head
```

3. API будет доступно по адресу: http://localhost:8000
4. Документация API (Swagger): http://localhost:8000/docs

### Локальная разработка (без Docker)

1. Установите зависимости:
```bash
cd backend
pip install -r requirements.txt
```

2. Убедитесь, что PostgreSQL запущен и создайте базу данных:
```bash
createdb tourist_app
```

3. Настройте переменные окружения в `backend/.env`

4. Примените миграции:
```bash
alembic upgrade head
```

5. Запустите сервер:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 📱 Telegram Mini App

### Настройка Mini App

После запуска системы следуйте инструкциям в файле **[TELEGRAM_SETUP.md](TELEGRAM_SETUP.md)** для создания и настройки Mini App в Telegram.

### Текущий статус

Проверьте файл **[STATUS.md](STATUS.md)** для просмотра текущего статуса всех компонентов.

---

## 📚 API Эндпоинты

### Аутентификация

- `POST /api/v1/auth/register` - Регистрация по email
- `POST /api/v1/auth/login` - Вход по email
- `POST /api/v1/auth/telegram` - Аутентификация через Telegram
- `POST /api/v1/auth/refresh` - Обновление токена
- `GET /api/v1/auth/me` - Получение текущего пользователя

### Города

- `GET /api/v1/cities` - Список всех городов
- `GET /api/v1/cities/{id}` - Информация о городе

### Достопримечательности

- `GET /api/v1/attractions` - Список достопримечательностей (с фильтрами и пагинацией)
- `GET /api/v1/attractions/{id}` - Детальная информация о достопримечательности

### Избранное

- `GET /api/v1/favorites` - Список избранных достопримечательностей
- `POST /api/v1/favorites` - Добавить в избранное
- `DELETE /api/v1/favorites/{id}` - Удалить из избранного

**Полная документация:** http://localhost:8000/docs

## Миграции базы данных

### Создание новой миграции

```bash
cd backend
alembic revision --autogenerate -m "Описание изменений"
```

### Применение миграций

```bash
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

## Разработка

### Стандарты кодирования

- Используйте `black` для форматирования кода
- Используйте `flake8` для проверки стиля
- Следуйте PEP 8

### Тестирование

```bash
pytest
```

## Лицензия

Проект находится в разработке.
